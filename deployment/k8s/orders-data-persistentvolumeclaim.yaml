apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: orders-data
  name: orders-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
